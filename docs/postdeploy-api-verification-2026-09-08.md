# Post-deploy API repairs — 8 September 2026

## Verified causes and scope

- Stories' live table lacked six safety fields already requested by FeedStoryRing. Existing fallback hid the persistent first-request 400. Applied narrow additive column compatibility, preserving existing rows; added a restrictive hidden-story read policy. The broader moderation/reporting stack is not activated by this migration.
- Eats recent orders selected nonexistent `store_id` and joined `store_profiles`. Live `food_orders_restaurant_id_fkey` references `restaurants(id)`. The query now selects that explicit relationship and remains filtered by authenticated customer ID; missing restaurant rows are skipped. React Query keys include the account; old responses cannot populate the new account key. Unavailable/retry is distinct from confirmed empty.
- Wallet's owner-masked reader did not exist. Added only `list_own_customer_payout_methods(text,uuid)` from the reviewed contract, adapted to live fields. It uses auth.uid(), a fixed empty search_path, denies anonymous execution, rejects invalid scopes, filters owner plus requested store, masks destination to four characters and excludes finance notes. Missing verified_at remains null. This does not activate broader cash-out/payment mutation features.
- `is_driver(uuid)` exists but authenticated execution is denied. This is an execution-grant issue, not proof of a missing driver account or wrong RLS table. Removed the inappropriate passenger-only rejection from the Media account hub; actual service authorization remains on the backend. No driver role grants or approval bypass.

Applied migration: `20260908225412_postdeploy_story_payout_read_contract.sql`, matching the timestamp recorded by Supabase MCP. Project: slirphzzwcogdbkeicff. No user/account/order/payment rows changed. Do not apply all pending repository migrations.

## Live read-only evidence

RLS remains enabled for stories and customer_payout_methods. RPC execute: anon=false, authenticated=true. Authenticated unrelated identity returns 0 rows. Projection checks returned 0 wrong-owner rows, 0 destinations longer than four characters and 0 unrelated-store rows. The payout table currently contains no rows, so these are empty-table boundary checks, not positive real-account payout fixture coverage. No synthetic financial/account records were inserted. SQL session role/claims were transaction-local and rolled back; no account was impersonated in a browser or session token minted.

REST selections with the same `Accept-Profile: public` header used by the app return HTTP200 for stories safety fields and the explicit food-order restaurant relationship (`limit=0`, no customer content fetched). The initial headerless probe used the API's other default schema and returned PGRST205; it is not evidence that the app's public-schema query fails.

## Monitoring

`scripts/monitoring/api-error-rates.mjs` queries the supported Management API `/analytics/endpoints/logs` using ClickHouse SQL. It retrieves only aggregate counts for the four allowlisted paths, stripping query strings before grouping. Threshold: at least 3 HTTP4xx and >=5% error rate in 15 minutes. Zero traffic is `rate:null`, not a health pass. Query/credential errors fail separately. Scheduled GitHub workflow runs every ten minutes and opens one deduplicated issue for a breach or unavailable monitor. No browser telemetry, IPs, identities, request bodies or tokens are collected.

The query ran successfully against production logs and detected the pre-repair payout errors. Existing SUPABASE_ACCESS_TOKEN GitHub secret is used; prefer a token limited to analytics_logs_read. The workflow must be published to the default branch before GitHub schedules it; local YAML is not an active alert service. No Telegram destination was supplied, so this uses GitHub alerts instead.

Docs: [Management logs API](https://supabase.com/docs/reference/api/v1-get-project-logs), [query fields and dialect](https://supabase.com/docs/guides/observability/advanced-log-filtering).

## Rollback and remaining boundaries

Roll back the web Worker to `abfb40f3-cf07-474c-a2ca-a5bf8f73d447` if needed. Leave additive columns/RPC in place during rollback; the previous client already expects them. Do not drop populated moderation columns. To disable the new reader in an incident, revoke its authenticated execute grant (Wallet then displays unavailable). Restore/review the read policy separately if needed; never broaden RLS to get a green response.

Identity provider activation, account merges, password rotation and SMS-provider delivery are not part of these repairs. See IDENTITY_PLAN.md. Ride/Driver changes are CI-only; their runtime deployments belong to concurrent tasks. Follow-up issues: [Ride #75](https://github.com/kimlainchhorng/ZIVO-ride/issues/75), [Driver #128](https://github.com/kimlainchhorng/zivodriver/issues/128).

Reviewable CI publication: [Media monitoring PR #236](https://github.com/kimlainchhorng/zivosmedia/pull/236), [Ride performance PR #76](https://github.com/kimlainchhorng/ZIVO-ride/pull/76), [Driver performance PR #129](https://github.com/kimlainchhorng/zivodriver/pull/129). These are draft branches; no default branch or sibling production site was changed. Media merge requires coordinating its default-branch release pipeline with the actual validated website source.

Offline PostgreSQL follow-up: `scripts/qa/postdeploy-read-contract-db.mjs` passed against PGlite 0.5.8 installed only under `/tmp/zivo-pglite-postdeploy`. The exact applied migration was executed against an in-memory fixture matching the relevant live columns, roles and RLS. Two synthetic accounts prove positive own-row reads, different masked destinations, denied cross-account/store reads, absence of raw bank/ABA identifiers and finance notes, null missing verified_at, rejected null/invalid scopes, anonymous/no-identity rejection, and hidden-story visibility only for the owner. No live fixtures, account tokens or provider requests were used. Run with `PGLITE_MODULE=/tmp/zivo-pglite-postdeploy/node_modules/@electric-sql/pglite/dist/index.js node scripts/qa/postdeploy-read-contract-db.mjs` after installing the pinned test dependency in that isolated prefix.

Final local release validation: `npm run update` passed after correcting the recovery/Login components to use the actual `useI18n().currentLanguage` contract. The focused recovery/identity suite passed 9/9, including Khmer retry text and held responses across account changes. The Login form now truthfully labels its email-password input; accepting typed phone text did not establish working phone authentication. No SMS capability was enabled. The npm registry interrupted the first advisory check; a retry reported zero vulnerabilities. The MCP migration-history evidence was refreshed from the live catalog after the new migration (1,781 remote migrations; latest 20260908225412), preserving strict checks.

The tighter Lighthouse gate passed all six runs (three per language) using the configured actual slow-4G / 4x CPU DevTools profile. EN: median LCP 1,574 ms, 30 requests, 312,593 bytes, CLS 0. KM: median LCP 1,592 ms, 32 requests, 398,366 bytes, maximum observed CLS 0.000637. These are measured local production-build results, not PageSpeed Insights simulated results. Reused the existing owned loopback preview by omitting only LHCI's duplicate server startup in a temporary config; all budgets and throttling stayed identical.

Strict technical preflight and release:gate passed; the fresh strict production summary was accepted. The existing owner-authorized deploy-first deferral covers only the still-missing registered address and support phone; the normal business-identity gate remains unchanged.

Production publication completed at 2026-09-08T23:40Z. Worker `zivo` version `6207f0b7-9f85-48af-a125-0977d176ebd2` is active at 100%, confirmed by the deployment list. All 12 live desktop/mobile public browser cases passed (1.1 minutes): EN/KM content and fonts, hydration, route navigation, cookie interaction, standalone stylesheet and no-JavaScript reading. Live mobile startup: EN 29 requests / 361,036 bytes; KM 31 / 449,198 bytes (under the limit, with little remaining transfer headroom). Apex HTTP200, unknown path HTTP404, and www HTTP301 preserving `/flights?lang=km` verified. Existing Worker bindings and variables were preserved. No Ride/Driver/Business runtime deployment was performed in this task.

Live login entry checked in fresh isolated browser contexts for EN and KM: account picker opens the email form, input type is email, labels are localized, and zero placeholder-host requests occur. The first probe did not wait for the asynchronously rendered picker and timed out; the corrected probe waited for picker/form visibility and passed both languages. No credentials were submitted. Dedicated QA-account authenticated browser flows and real positive payout records remain unverified; privacy behavior is covered by the isolated PostgreSQL fixtures described above.
