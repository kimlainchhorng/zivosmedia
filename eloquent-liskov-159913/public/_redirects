# Channel share previews: proxy /share/c/:handle to the channel-og edge function
# so iOS / iMessage / Slack see meta tags on the brand domain (not supabase.co).
# Browsers visiting this URL receive HTML with an inline <script> that bounces
# them to /c/:handle (the SPA), so no redirect loop.
/share/c/:handle  https://slirphzzwcogdbkeicff.supabase.co/functions/v1/channel-og?handle=:handle  200!

# SPA fallback
/* /index.html 200
