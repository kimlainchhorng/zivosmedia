# Copy this file to android/keystore.properties on the release-signing machine.
# Never commit android/keystore.properties or the real keystore file.
#
# The storeFile path is relative to the android/ directory.
storeFile=app/release.keystore
storePassword=<owner-controlled password>
keyAlias=<owner-controlled alias>
keyPassword=<owner-controlled password>
