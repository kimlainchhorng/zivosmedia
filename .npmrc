# @vitejs/plugin-react's optional @babel/plugin-transform-runtime peer chain
# trips npm's strict resolver (ERESOLVE on a peerOptional edge). Legacy
# resolution ignores optional-peer conflicts and matches what the lockfile
# records. See 2026-09-01 lockfile regeneration after the stash-conflict fix.
legacy-peer-deps=true
