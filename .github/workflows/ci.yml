name: CI

# Full type-check + lint + unit tests + production build on every push/PR.
# Blocks merge if any step fails.

on:
  push:
    branches: [main]
    paths-ignore:
      - "docs/**"
      - "website/**"
      - "**/*.md"
      - "**/*.log"
      - "booking-*.json"
  pull_request:
    branches: [main]
    paths-ignore:
      - "docs/**"
      - "website/**"
      - "**/*.md"
      - "**/*.log"
      - "booking-*.json"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: Type-check · Lint · Test · Build
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Set up Node.js 24
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version-file: ".nvmrc"
          cache: npm

      - name: Reject committed merge markers
        run: npm run qa:conflict-markers

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      # ── TypeScript ──────────────────────────────────────────────────────────
      - name: TypeScript type-check
        env:
          NODE_OPTIONS: "--max-old-space-size=8192"
        run: npm run type-check

      - name: Cloudflare Worker type-check
        run: npm run type-check:worker

      # ── Lint ────────────────────────────────────────────────────────────────
      - name: ESLint
        run: npm run lint

      # ── Database migration safety ───────────────────────────────────────────
      - name: Supabase migration drift check (PR advisory)
        if: github.event_name == 'pull_request'
        run: npm run supabase:migrations:check
        continue-on-error: true

      - name: Supabase migration drift check (main strict)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: npm run supabase:migrations:check:main
        continue-on-error: true

      # ── Unit tests ──────────────────────────────────────────────────────────
      - name: Vitest unit tests
        run: npm run test
        env:
          NODE_OPTIONS: "--max-old-space-size=8192"

      # ── Production build ────────────────────────────────────────────────────
      - name: Production build
        env:
          NODE_OPTIONS: "--max-old-space-size=8192"
        run: npm run build

      # ── Upload build artifacts (for other jobs or debugging) ────────────────
      - name: Upload dist
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: dist-${{ github.sha }}
          path: dist/
          retention-days: 3
          if-no-files-found: error
